const config = require('../../config.json')
module.exports = {
    name: "volume",
    category: "Music",
    description: "Gérer le volume de la chanson",
    example: `${config.Prefix}volume`,
}   
