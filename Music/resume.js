const config = require('../../config.json')

module.exports = {
    name: "resume",
    category: "Music",
    description: "Reprendre le morceau en cours de lecture",
    example: `${config.Prefix}resume`,
}   
