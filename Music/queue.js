const config = require('../../config.json')
module.exports = {
    name: "queue",
    category: "Music",
    description: "Obtenez tous les noms de chansons qui sont dans la file d'attente",
    example: `${config.Prefix}queue`,
}   
