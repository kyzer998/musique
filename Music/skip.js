const config = require('../../config.json')
module.exports = {
    name: "skip",
    category: "Music",
    description: "Skip la chanson ou passez à la chanson suivante",
    example: `${config.Prefix}skip`,
}   