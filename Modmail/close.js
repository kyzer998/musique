const config = require('../../config.json');

module.exports = {

    name: "close",
    category: "Modmail",
    description: "Fermer la discussion en mp (Admin)",
    example: `${config.Prefix}close`,
}    